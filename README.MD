# Nyssebot

## Dependencies
```
mkdir libs
git clone 
```

brainboxdotcc/DPP
libfmt/fmt
libcpr/cpr

## Building
How to build:
```cpp
cmake -S . -B build
cmake --build build
cmake --build build -t test
```